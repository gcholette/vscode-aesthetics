const app_prefix = "[VS Code Aesthetics]"

const Msgs = {
  enable_all: `${app_prefix} Enable All activated.`,
  enable_glow: `${app_prefix} Enable Glow activated.`,

  debug_1: (x: string) => `${app_prefix} [DEBUG] ${x}`,
}

export default Msgs
